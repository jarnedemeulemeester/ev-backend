[project]
name = "ev-backend"
version = "0.1.0"
description = "Backend for EV charging management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.2",
    "ocpp>=2.1.0",
    "pydantic-settings>=2.12.0",
    "uvicorn[standard]>=0.38.0",
]
